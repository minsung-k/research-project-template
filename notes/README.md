# Notes — research process artifacts

Research-process phase outputs live here (`problem.md`, `phase1_commitments.md`,
`litreview.md`, `methods_*.md`, `method_v1.md`, etc.). See `CLAUDE.md` for
the phase-by-phase output convention.

Subdirectories prefixed `dryrun_<date>/` are frozen archives of prior cycles —
agents should not read them (see `CLAUDE.md` universal hard rule 5).
