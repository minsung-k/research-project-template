# Research log

Append to the top. Newest entry first. Keep entries terse. Cross-reference run dirs by their full name (`2026-05-06_<user>_<exp>_seed0`).

The agent reads this at session start to figure out where things are. You read it after vacation to remember where you were.

---

## YYYY-MM-DD — project bootstrapped from research-project-template
- Repo skeleton in place. Run `source setup_env.sh --create` to create the venv, then `pytest tests/ -x` to smoke-test.
- Next: rename `src/foo/` to your project name, fill in `CLAUDE.md` "What this project is", start Phase 1 (`notes/problem.md`).

<!-- template entry — copy and fill in:
## YYYY-MM-DD — short title
- What I tried:
- What happened (point at run dir):
- What I learned:
- Next:
-->
