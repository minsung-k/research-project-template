# configs/sweep/lr_seed.yaml
# Example sweep spec consumed by scripts/sweep.py
name: lr_seed
base:
  exp_name: lr_seed_sweep
grid:
  seed: [0, 1, 2]
  optimizer.lr: [1.0e-3, 3.0e-4, 1.0e-4]
array:
  max_concurrent: 4
